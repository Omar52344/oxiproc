Oxiproc 🦀 — Lightweight Process GuardianOxiproc es un administrador de tareas y monitor de sistema basado en terminal (TUI) desarrollado en Rust. Diseñado específicamente para entornos donde el rendimiento es crítico y el Administrador de Tareas de Windows resulta demasiado pesado o intrusivo.A diferencia de las herramientas tradicionales, Oxiproc categoriza inteligentemente los procesos para evitar cierres accidentales que puedan comprometer la estabilidad del sistema operativo.✨ Características PrincipalesBajo Consumo de Recursos: Ejecución ultraligera (< 10MB RAM) ideal para servidores y máquinas con recursos limitados.Diferenciación Inteligente: Identifica visualmente procesos de Sistema (Críticos) vs. Usuario (Seguros de cerrar).Protección de Criticidad: Bloqueo preventivo de la función "Kill" para hilos vitales del Kernel y servicios esenciales.Interfaz de Terminal (TUI): Gráficos de alta fidelidad sin necesidad de una interfaz gráfica pesada.Portable: Binario único sin dependencias externas.🛠️ Arquitectura del SistemaLa arquitectura sigue el patrón de Monitorización Desacoplada, separando la recolección de métricas de la renderización visual para garantizar que el monitor no afecte los resultados de la medición.Definición Técnica (Prompt de Arquitectura)"Arquitectura de un monitor de procesos TUI en Rust utilizando un modelo de hilos separado. El Data Provider utiliza la crate sysinfo para consultar el kernel cada $n$ milisegundos, filtrando los procesos mediante un Categorizer Engine que valida el UID/Owner y el nombre del ejecutable contra una lista blanca de servicios críticos del SO. Estos datos se envían a través de un canal asíncrono (mpsc) al UI Renderer basado en ratatui, que gestiona el estado de la interfaz y la entrada del usuario (crossterm) para permitir la interacción segura con la lista de procesos."🚦 Clasificación de ProcesosNivelTipoDescripciónAcción Permitida🔴 0SistemaProcesos vitales (Kernel, RPC, Init).Bloqueado🟡 1ServicioServicios de fondo y drivers.Confirmación Requerida🟢 2UsuarioAplicaciones de usuario (Navegadores, Editores).Libre🚀 Instalación y CompilaciónAsegúrate de tener instalado el toolchain de Rust (edición 2021 o superior).Clonar el repositorio:Bashgit clone https://github.com/tu-usuario/oxiproc.git
cd oxiproc
Compilar para producción:Bashcargo build --release
Ejecutar:Bash./target/release/oxiproc
🛡️ ¿Por qué Rust?El Administrador de Tareas de Windows utiliza una gran cantidad de llamadas a la API de GDI/DirectX para dibujar su interfaz, lo que consume ciclos de CPU innecesarios. Oxiproc se comunica directamente con el sistema operativo y renderiza solo texto plano enriquecido, eliminando el overhead gráfico por completo.📄 LicenciaEste proyecto está bajo la Licencia MIT - consulta el archivo LICENSE para más detalles.